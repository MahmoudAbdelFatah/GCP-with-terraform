apiVersion: v1
kind: PersistentVolume
metadata:
  name: jenkins-pv
  namespace: develop-area
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  nfs:
    server: nfs01.thoutam.loc
    path: "/mnt/jenkins"
